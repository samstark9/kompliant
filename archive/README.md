# Archive

Reviewed drafts land here, exactly as they were reviewed, date-stamped by `tools/archive.py` with the day they left the inbox. Each pairs with its review in `reviews/`. The inbox holds only drafts awaiting review.
